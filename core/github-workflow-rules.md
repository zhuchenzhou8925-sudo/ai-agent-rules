# GitHub 工作流规则

## 基础规则
1. 不得上传敏感文件。
2. 不得直接 push 到 main，除非是首次初始化且用户明确确认。
3. 后续修改优先进入 `draft/*` 分支。
4. push 前必须输出上传前确认报告。
5. PR 前必须输出确认报告。
6. 合并 main 前必须等待用户确认。
7. 如发现疑似敏感文件，必须停止。
8. 对非技术用户，Codex 必须用中文解释修改内容、影响范围、风险等级和回滚方式。
9. 工具类脚本默认应支持 dry-run。
10. 不得修改现有本机整体工作区结构。
